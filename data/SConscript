Import('env')

images = Split("""
	abraca.svg
	abraca-128.png
	abraca-playlist-22.png
	abraca-collection-22.png
""")

env.Alias('install',
	env.Install('$PREFIX/share/pixmaps', images)
)

env.Alias('install',
	env.Install('$PREFIX/share/applications', 'abraca.desktop')
)
