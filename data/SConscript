Import('env')

env.Alias('install', env.Install('$prefix/share/pixmaps', 'abraca.svg'))
env.Alias('install', env.Install('$prefix/share/pixmaps', 'abraca-128.png'))
env.Alias('install', env.Install('$prefix/share/applications', 'abraca.desktop'))
